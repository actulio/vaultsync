/**
 * STUB — Task 9 will replace this with real OAuth implementation.
 * Exists so Task 8 onboarding screens typecheck without Task 9 being complete.
 */

/**
 * Sign in with Google and authorise Drive access.
 * Returns true on success, false on cancellation or error.
 */
export async function signInWithGoogle(): Promise<boolean> {
  return false;
}

/**
 * Record that the user chose to connect Drive later.
 * Task 9 will persist this preference and prompt again on next launch.
 */
export function skipDriveForNow(): void {
  // no-op stub
}
