# Expo / React Native require a flat node_modules layout for autolinking and
# Metro to resolve native modules. pnpm's default symlinked store breaks this,
# so use the hoisted node-linker (behaves like npm/yarn).
node-linker=hoisted
